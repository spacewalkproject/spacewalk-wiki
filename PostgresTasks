[wiki:PostgreSqlProject Main Project Page]

== Project Tasks ==

Project tasks are work items that need to be tracked.  They are different then issues in that items listed here
simply need to be completed and are not necessarily risks to the project.

=== General ===
||'''Name'''||'''Description'''||'''Status'''||'''Assigned'''||'''Notes'''
||Dead Query Script||Write a script to: find queries defined but not referenced in perl code; find queries defined but not referenced in java code; find queries defined in both (perl migrated to java).|| done ||dgoodwin|| determined that the number of queries involved did not warrant the risk ||
||Query Script||Enhance script to list files containing SQL; provides file and query counts|| done || zeus || [attachment:file_query_count script]||
||Query File List||Publish ''final'' list of files containing queries that need to be tagged.  This item will be created by merging script task(s) output|| || || ||
||Add comments||Add SQL comments to every query|| || bmomjian || ||
||Postgres Schema||Create test version of Postgres schema, for testing of queries|| || jortel || Using advanced server ||
||Database Versions||What is the minimum supported version of  Postgres?|| ||  || ![1] This will depend on targeted versions of RHEL||
||Custom Data Type||Add EVR_T data type with aggregate|| ||  || ||
||Prepare git branch||Either clean up the existing postgres branch, or create a new branch and ''cherry pick'' Devin's changes|| ||dgoodwin|| Would be good to have this by COB 12/19 ||
||Postgres Dev Packages||Create a devel yum repo containing custom packages required for satellite to use __both__ oracle and postgres|| || dgoogwin|| ||
||Postgres DDL|| Publish an initial postgres schema generated by oratopg  || || dgoodwin,bmomjian || To be cleaned up by bmomjian ||
||Sample Dataset||Create a sample database dataset that can be loaded in both oracle and postgres to be used by developers|| || jortel||||
||Monitoring||Are we converting the monitoring module?|| ||  || ||
||Remove dead perl queries|| Remove the dead perl queries from git || || dgoodwin || ||
||Orafce|| Orafce requires Postgres 8.2; port Orafce to Postgres 8.1 and perhaps 7.4; packaging issues? || || bmomjian || ||
|| Setup schema review meeting || Have developers review schema beforehand, meet to discuss core tables and answer questions with as many developers as possible. || || jortel || ||
|| db-control || what about the RHN db-control stuff.  It's probably oracle based || || || ||
=== Queries ===
||'''Name'''||'''Description'''||'''Status'''||'''Assigned'''||'''Notes'''
||Sysdate|| Convert sysdate to appropriate current_date and current_timestamp || || || ||
||Sysdate addition|| Add Postgres timestamp + float compatibility operator || || || ||
||nextval()|| Handle nextval() differences || || || ||

=== Notes ===
 ![1]:: 
 RHEL-4 includes postgres 7.4 and RHEL-5 includes postgres 8.1.  The latest postgres is 8.3.8.
 7.4 has the following limitations (Exceptions, Save Points) appear to be applicable to spacewalk.

